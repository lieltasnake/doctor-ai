# Doctor AI - Frontend

This directory contains the React Native mobile application for Doctor AI.

## Purpose
- Manages the user interface (UI) and user experience (UX).
- Handles user interactions (text and voice input).
- Communicates with the backend API to send symptoms and receive AI recommendations.
- Manages local state and navigation between screens (Login, Register, Chat, Profile).
